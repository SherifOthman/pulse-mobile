import { Text, View } from "react-native";

export default function Doctors() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-accent">doctors</Text>
    </View>
  );
}
